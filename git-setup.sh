git config --global user.email "dreed@nomadia.org"
git config --global user.name "Damon Reed"
git config --global pull.rebase false
git clone https://github.com/damonreed/utilities.git
cd utilities
git remote -v
git branch -a